name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - uses: docker://salesforce/salesforcedx
    - name: Where Am I??
      run: pwd && ls
    - name: Test Docker
      run: |
        docker images
        docker run salesforce/salesforcedx
        sfdx
